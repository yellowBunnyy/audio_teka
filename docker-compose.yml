version: "3.3"
services:
  fast_api_service:
    build: .
    command: 'uvicorn src.entrypoints.my_app:app --host localhost --port 5000'
    ports:
      - 5000:5000
    environment:
      - DB_URI=postgresql://krzeemo:test1@db/audio
    depends_on:
      - db
    volumes:
      - .:/usr/my_app
      - ./src/:/src/
      - ./tests:/tests
    links:
      - db
  db:
    image: 'postgres:latest'
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=krzeemo 
      - POSTGRES_PASSWORD=test1 
      - POSTGRES_DB=audio

